# EXAMPLE USAGE:
#
#   Refer for explanation to following link:
#   https://lefthook.dev/configuration/
#
# pre-push:
#   jobs:
#     - name: packages audit
#       tags:
#         - frontend
#         - security
#       run: yarn audit
#
#     - name: gems audit
#       tags:
#         - backend
#         - security
#       run: bundle audit
#
# pre-commit:
#   parallel: true
#   jobs:
#     - run: yarn eslint {staged_files}
#       glob: "*.{js,ts,jsx,tsx}"
#
#     - name: rubocop
#       glob: "*.rb"
#       exclude:
#         - config/application.rb
#         - config/routes.rb
#       run: bundle exec rubocop --force-exclusion {all_files}
#
#     - name: govet
#       files: git ls-files -m
#       glob: "*.go"
#       run: go vet {files}
#
#     - script: "hello.js"
#       runner: node
#
#     - script: "hello.go"
#       runner: go run

commit-msg:
  parallel: true
  commands:
    message-style:
      run: pnpm exec commitlint --edit
    message-spelling:
      run: pnpm exec cspell --no-summary {1}

# lint staged
pre-commit:
  parallel: true
  commands:
    commit-spelling:
      glob: '*.{js,ts,jsx,tsx,md,json}'
      run: pnpm exec cspell {staged_files}
    commit-signing:
      run: |
        if ! git log -1 --pretty=%G? HEAD | grep -q "[GU]"; then
          echo "❌ Commit is not signed or signature is invalid!"
          exit 1
        fi
    commit-link-rot:
      glob: '*.md'
      run: pnpm exec markdown-link-check {staged_files}

# depcheck, typesync, tests, full project lint, build
#pre-push:
